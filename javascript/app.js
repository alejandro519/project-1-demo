// Get click event and generate a function
    // Get city from user's input #search-input store it in variable searchInput
    // Generate the URL concatenating the searchInput where the "city" term goes

    // Generate a search request from the API using ajax... then...
        // Store the first 5 objects into an array of objects called events[];
        
    // Display with classes: result-name, result-date, result-venue, result-artist, result-description, and result-poster into the result-section

